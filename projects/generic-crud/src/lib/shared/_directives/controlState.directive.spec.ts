/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ControlStateDirective } from './controlState.directive';

describe('Directive: ControlState', () => {
  it('should create an instance', () => {
    const directive = new ControlStateDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
