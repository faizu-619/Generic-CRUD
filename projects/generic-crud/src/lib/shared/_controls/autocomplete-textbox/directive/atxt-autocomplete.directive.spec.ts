import { AtxtAutocompleteDirective } from './atxt-autocomplete.directive';

describe('AtxtAutocompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new AtxtAutocompleteDirective();
    expect(directive).toBeTruthy();
  });
});
